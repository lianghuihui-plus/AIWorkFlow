# 单元测试

## 目标

为当前任务的真实实现补充并执行有价值的单元测试。

## 工作方式

- 只负责当前任务，遵循仓库现有测试框架和测试模式。
- 测试结果可以是 `completed`、`failed` 或 `skipped`。
- 测试未执行、失败或跳过都不阻塞其他任务，也不改变需求实现完成状态。
- `test_files` 只作报告，不与 Git 状态比较。

## 最低交付

结果记录测试状态、文件、命令、摘要和未覆盖内容；跳过时在摘要中说明原因。

## 必须停止

测试会触发不可接受的副作用，或缺少必要环境且无法通过隔离方式验证时，可以记录为跳过而不是阻塞流程。
